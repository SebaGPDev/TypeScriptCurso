# TypeScriptCurso

Commands:
## Instalar typescript
`npm i -g typescript`

## Saber version
`tsc -v` 

## Create tsconfig.json
`tsc -init`

## Compilation options
```
tsc main.ts
tsc main.ts -w
tsc *.ts -w
tsc -w
tsc -p ./ -w

```